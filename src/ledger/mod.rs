// src/ledger/mod.rs
// Aluminum OS — Ledger Module
//
// Exposes the provenance sub-module which maps the philosophical
// `ProvenanceTrailer` concept to the concrete `GoldenTrace` git trailer.

pub mod provenance;
